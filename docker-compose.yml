services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data        # SQLite database persists here
    env_file: .env
    environment:
      DATABASE_URL: /app/data/yt-player.db
